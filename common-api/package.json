{
  "name": "common-api",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "dependencies": {
    "@chevtek/poker-engine": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.2"
  },
  "scripts": {
    "clean": "rimraf ./build",
    "build": "npm run clean && tsc"
  }
}
